# Classes reference

`classes` is a Tailwind class map controlling layout.

Key groups:

- Toast: `toastBase`, `title`, `message`, `closeBtn`, progress classes
- Buttons: `btnBase`, `btn.{variant}`
- Dialogs: `modal*` classes
